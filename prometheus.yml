global:
  scrape_interval: 5s # Fréquence de collecte des données

scrape_configs:
  - job_name: 'flask-app'
    static_configs:
      - targets: ['web-app:5000'] # 'web-app' sera le nom de notre conteneur Flask dans Docker
